<Page x:Class="Gestion.presentation.views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Gestion.presentation.views"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="LoginPage">

    <Grid Background="#FFF" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Height="434" Width="786">
            <TextBlock Text="Inicio de sesión" Margin="0, 0, 0, 5" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Usuario: " Margin="0, 0, 5, 0"/>
                <TextBox Text="Usuario" Margin="0,0,0,5" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0, 0, 0, 5" HorizontalAlignment="Center">
                <TextBlock Text="Contraseña: " Margin="0, 0, 5, 0"/>
                <TextBox Text="Contraseña" Margin="0,0,0,5" />
            </StackPanel>
            <Button Content="Iniciar sesión" Click="BtnInicioSesion_Click" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
